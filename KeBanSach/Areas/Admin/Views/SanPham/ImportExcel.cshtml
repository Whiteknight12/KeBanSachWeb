<div class="container mt-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white text-center">
            <h2>Nhập File Excel</h2>
            <p class="mb-0">Tải lên tệp Excel để thêm dữ liệu một cách nhanh chóng</p>
        </div>
        <div class="card-body">
            <form method="post" enctype="multipart/form-data" class="text-center">
                <div class="mb-4">
                    <label for="Inputfile" class="form-label text-muted">Chọn file Excel:</label>
                    <input type="file" name="Inputfile" id="Inputfile" class="form-control w-50 mx-auto" />
                </div>
                <button type="submit" class="btn btn-success px-5">
                    <i class="bi bi-upload"></i> Import
                </button>
            </form>
        </div>
    </div>
</div>